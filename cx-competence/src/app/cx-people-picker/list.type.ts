export enum ListType {
  /**
   * Using for picking up new people.
   */
  PickingPeople,

  /**
   * Using for showing selected people including ability for removing the selection.
   */
  ViewingSelectedPeople,
}
