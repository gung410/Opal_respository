---
format_version: 3
environments:
  systemtest:
    pipelines:
      - cx-tagging-api-build
      - cx-tagging-api-systemtest
  uat:
    pipelines:
      - cx-tagging-api-uat
  pre-production:
    pipelines:
      - cx-tagging-api-pre-production
  production:
    pipelines:
      - cx-tagging-api-production
pipelines:
  cx-tagging-api-build:
    group: cx-tagging-api
    label_template: "release-2.0.${COUNT}"
    lock_behavior: none
    parameters: # list of parameters that can be configured for a pipeline
      PROJECT_DIR: src/microservices/tagging-microservice/Api/
      SONAR_PROJECTKEY: cx-tagging-api-release
      SONAR_SOLUTION_FILE: PlatformWorkspace.sln
    environment_variables:
      DOCKER_IMAGE_TAG:
      DOCKER_IMAGE_REPO: cx-tagging-api
    template: osd-linux-docker-build-sonar-dotnetcore
    materials:
      bitbuket-git:
        branch: releases/2.0
        git: "https://bitbucket.org/cxdev/cx-opal20-platform.git"
        username: samnguyenn
        encrypted_password: AES:Gl+5mhjKWgWeGW5TCVLRlA==:xT2hvR8chS49PNkvuN2xhw==
        whitelist:
          - src/microservices/tagging-microservice/Api/**/*.*
  cx-tagging-api-systemtest:
    group: cx-tagging-api
    label_template: "${upstream}"
    materials:
      upstream:
        pipeline: cx-tagging-api-build
        stage: docker-push
    parameters: # list of parameters that can be configured for a pipeline
      UP_STREAME_PIPELINE_NAME: cx-tagging-api-build
      UP_STREAME_PIPELINE_STAGE: docker-push
      UP_STREAME_PIPELINE_JOB: docker-push
    environment_variables:
      DOCKER_IMAGE_TAG:
      DOCKER_IMAGE_REPO: cx-tagging-api
    template: ansible-deploy-fetch-upstream
  cx-tagging-api-uat:
    group: cx-tagging-api
    label_template: "${upstream}"
    materials:
      upstream:
        pipeline: cx-tagging-api-systemtest
        stage: deploy-stage
    parameters: # list of parameters that can be configured for a pipeline
      UP_STREAME_PIPELINE_NAME: cx-tagging-api-systemtest
      UP_STREAME_PIPELINE_STAGE: deploy-stage
      UP_STREAME_PIPELINE_JOB: deploy
    environment_variables:
      DOCKER_IMAGE_TAG:
      DOCKER_IMAGE_REPO: cx-tagging-api
    template: ansible-deploy-fetch-upstream
  cx-tagging-api-pre-production:
    group: cx-tagging-api
    label_template: "${upstream}"
    materials:
      upstream:
        pipeline: cx-tagging-api-uat
        stage: deploy-stage
    parameters: # list of parameters that can be configured for a pipeline
      UP_STREAME_PIPELINE_NAME: cx-tagging-api-uat
      UP_STREAME_PIPELINE_STAGE: deploy-stage
      UP_STREAME_PIPELINE_JOB: deploy
    environment_variables:
      DOCKER_IMAGE_TAG:
      DOCKER_IMAGE_REPO: cx-tagging-api
    template: ansible-deploy-fetch-upstream
  cx-tagging-api-production:
    group: cx-tagging-api
    label_template: "${upstream}"
    materials:
      upstream:
        pipeline: cx-tagging-api-uat
        stage: deploy-stage
    parameters: # list of parameters that can be configured for a pipeline
      UP_STREAME_PIPELINE_NAME: cx-tagging-api-uat
      UP_STREAME_PIPELINE_STAGE: deploy-stage
      UP_STREAME_PIPELINE_JOB: deploy
    environment_variables:
      DOCKER_IMAGE_TAG:
      DOCKER_IMAGE_REPO: cx-tagging-api
    template: ansible-deploy-fetch-upstream
